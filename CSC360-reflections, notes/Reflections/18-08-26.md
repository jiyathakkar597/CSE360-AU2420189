## Topics covered (revision checklist)
- [ ] Get Maven installed and make sure it actually builds Java projects on my machine
- [ ] Figure out what Maven is really doing under the hood — dependency management and automating the build, from what I understand
- [ ] Walk through setting up a JavaFX project with Maven inside IntelliJ
- [ ] Get a JavaFX program running end-to-end through Maven
- [ ] Nail down fill vs. stroke in JavaFX — what makes a shape "hollow" vs. "filled" in code
- [ ] Understand how position (layoutX/layoutY) and size (width/height) actually control where a shape sits

---

## What we did in class
Most of this session was setup rather than theory. I Maven installed and working — IntelliJ's bundled version handled it, though it can also be installed system-wide (e.g., via Homebrew). From there we used Maven inside IntelliJ to build and run a JavaFX program. There wasn't much new conceptual material — it was mostly getting the tooling to actually work.

---
## Project completed
I put together a small JavaFX 2D program using Maven that draws a square and then pushed it to my git repo 'in class project'.
