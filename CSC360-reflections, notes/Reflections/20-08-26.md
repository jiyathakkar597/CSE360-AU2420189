## Topics covered (revision checklist)
- Disscused what is the DOs and DONTs to maintain the reflection notes for the lecture in the respective git repos. for eg: Convert any `.txt` reflection files to `.md` for better readability and GitHub preview
- Install and explore a dedicated Markdown viewer (e.g., Mark Text) for local previewing
- Look up and study the proof of the boundary method for generating valid triangle side lengths
- Research topics discussed in class as ongoing notes: triangle inequality, perpendicular lines, line intersections, generating valid triangles
- Study working Java graphics code between classes and come prepared with specific questions

## What we did in class

- Covered Markdown file organization and image embedding: dedicated `images/` folder, consistent file naming, and embedding with `![]()` using relative paths — which render correctly both on a cloned repo and on GitHub
- Covered math in Markdown using KaTeX: `$$` for block/display math, single `$` for inline math, and noted that AI tools or MathpixSnip can help generate KaTeX from text or images
- Covered Markdown viewers: VS Code's built-in preview plugins, and dedicated tools like Mark Text for editing + previewing on Windows; reinforced that `.md` is strongly preferred over `.txt`
- Covered reflection journal quality expectations: reflections should be specific (not vague like "I learned about computer graphics"), should name concrete concepts (vectors, curves, transformations, positioning, scaling), should be scannable with clear structure, should include questions to bring back to class, and file/directory naming should stay lowercase and consistent
- Covered Java Swing basics: `JFrame` + `JPanel` structure, overriding `paintComponent(Graphics g)`, the automatically-passed `Graphics` object, calling `super.paintComponent(g)` first, `setColor()` as a persistent pen state, and `drawRect(x, y, width, height)` parameters
- Covered anonymous classes in Java: inline subclass definition with no name, the `new ParentClass() { @Override ... }` syntax, and related concepts (nested, static nested, and inner classes)
- Covered the math behind triangles and lines in computer graphics: deriving a triangle from three line equations via pairwise intersection points, the triangle inequality, and the boundary method (`|A - B| < C < A + B`) for generating valid side lengths

## Project completed

- No project was built or pushed this session — the class content was conceptual/organizational rather than hands-on coding
