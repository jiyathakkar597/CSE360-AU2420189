## Topics covered (revision checklist)

- Install and set up Java (JDK) on the system
- Verify a Java installation is working via Command Prompt
- Create and run a Java project in IntelliJ using the Maven file format
- Understand basic Git commands: `add`, `commit`, `push`
- Understand how to troubleshoot common Java and Git errors

## What we did in class

- Downloaded and set up Java, then confirmed it was working via Command Prompt
- Created and ran a Java project in IntelliJ using Maven
- Learned and used basic Git commands (`add`, `commit`, `push`) to version-control the project
- Ran into and fixed a few basic Java and Git errors along the way

## Project completed

- Built a simple Java program to draw a square
- Successfully pushed the Java program to a GitHub repository
